spring:
  datasource:
    url: jdbc:mysql://mysqldb:3306/shop?allowPublicKeyRetrieval=true
    username: root
    password: admin_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
